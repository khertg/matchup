# Optional cloud sync and live viewer page (see supabase/README.md).
# Copy this file to .env.local and fill in your own Supabase project's values.
# Use the PUBLIC anon key only. Never put a service-role key in this app.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
