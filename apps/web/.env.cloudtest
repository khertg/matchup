# Fake values used only by `vite build --mode cloudtest` for the mocked-cloud E2E tests.
# Requests to this address are intercepted by Playwright; nothing real is contacted.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=test-anon-key
